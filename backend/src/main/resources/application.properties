# Server Configuration
server.port=8080
spring.application.name=healthcare-chat-assistant

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/healthcare_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Shankar@2005@31
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=healthcare-chat-assistant-super-secret-key-for-jwt-token-signing-256bits
jwt.expiration=86400000

# OpenAI Configuration (used only when ollama.enabled=false)
openai.api.key=your-openai-api-key-here
openai.model=gpt-4

# Ollama Configuration (local AI model)
ollama.enabled=true
ollama.base.url=http://localhost:11434
ollama.model=phi3:mini

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
upload.directory=C:/Users/sanka/OneDrive/Desktop/capstone/uploads/

# Tesseract Configuration
tesseract.data.path=C:/Program Files/Tesseract-OCR/tessdata

# CORS Configuration
cors.allowed.origins=http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:3000

# Logging
logging.level.com.healthcare=DEBUG
logging.level.org.springframework.security=DEBUG
